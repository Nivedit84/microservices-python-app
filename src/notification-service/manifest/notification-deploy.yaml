apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification
  labels:
    app: notification
spec:
  replicas: 2
  selector:
    matchLabels:
      app: notification
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 8
  template:
    metadata:
      labels:
        app: notification
    spec:
      containers:
        - name: notification
          image: soninivedit74/notification:v1.0
          ports:
            - containerPort: 8000
          envFrom:
            - configMapRef:
                name: notification-configmap
            - secretRef:
                name: notification-secret
